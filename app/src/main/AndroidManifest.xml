<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.caosapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_store_24"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaosApp">
        <activity android:name=".VistaComprarrActivity"></activity>
        <activity android:name=".VistaCategoriaNinossActivity" />
        <activity android:name=".VistacategoriamujeressActivity" />
        <activity android:name=".VistaCategoriaHombresActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>